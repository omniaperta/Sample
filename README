Usage
-----

cat sql/openeyes.sql | mysql -u root -p openeyes

Notes
-----
- The sample data is currently located in the sql folder as a single mysql dump
file. The import process will truncate existing tables and replace them with the
sample data.

